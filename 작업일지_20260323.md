# ShortsMake 작업일지

**날짜**: 2026-03-23
**프로젝트**: ShortsMake (`/home/comnict/aiwork/shortsmake`)
**스택**: FastAPI + Python (Port 8001) / React 19 + TypeScript + Vite (Port 5174)

---

## 작업 목록

### 1. 로컬 서버 재기동
- `.venv` 활성화 후 uvicorn 실행 (`source .venv/bin/activate`)
- venv PATH 미활성화 시 yt-dlp 경로 못 찾는 문제 확인

---

### 2. 분석 시작 "Not Found" 에러 수정
- **원인**: 서버 재시작 후 인메모리 job 데이터 소멸
- **수정**: `_restore_jobs()` 함수 추가 → 서버 기동 시 workspace 디렉토리 스캔하여 작업 자동 복원

---

### 3. 자막 정확도 개선
- **원인**: `initial_prompt` 설정으로 인해 Whisper가 프롬프트 텍스트를 자막으로 그대로 출력하는 환각 현상 (30초 영상에 20자만 추출)
- **수정** (`transcriber.py`):
  - `initial_prompt` 제거
  - `language=None` (자동 감지)
  - `log_prob_threshold=-1.0` 완화
  - 환각 패턴 필터 (`HALLUCINATION_PATTERNS`) 추가
  - 후처리 함수 `_postprocess_subtitles` 추가
- **결과**: 한국어 99.4% 감지, 자막 정상 추출

---

### 4. GPT 자막 리라이팅 기능 추가
- **신규 파일**: `server/services/rewriter.py`
  - gpt-4o-mini 기반 리라이팅
  - 스타일 6종: funny, dramatic, cute, professional, meme, custom
  - 타이밍 유지, JSON 파싱
- **API 추가** (`main.py`):
  - `POST /api/jobs/{id}/subtitle/{seg_id}/rewrite`
  - `GET /api/rewrite/styles`
- **GPT API 키 경로**: `server/.env` → `OPENAI_API_KEY=sk-...`
- **프론트엔드** (`SubtitleStep.tsx`): 리라이팅 미리보기 + `<input>`으로 직접 수정 + "적용하기" 버튼

---

### 5. 화면 비율 조정 기능 추가
- **스키마** (`schemas.py`): `EffectsConfig`에 `aspect_ratio: str = "9:16"` 추가, `ASPECT_RATIOS` 딕셔너리 (9:16, 1:1, 4:5, 16:9, 4:3)
- **프론트엔드** (`EffectsStep.tsx`): 비율 5종 프리셋 UI

---

### 6. 자막 폰트 변경
- 지마켓산스 볼드 적용: `GmarketSansTTFBold.ttf` 다운로드 후 `server/fonts/` 경로에 배치
- `SubtitleStyle` 기본값: `font_name="GmarketSansTTFBold"`, `font_size=44`, `outline_width=3`

---

### 7. 이전 완료 작업 불러오기 기능 추가
- **프론트엔드** (`InputStep.tsx`):
  - `api.listJobs()` 로 작업 목록 표시
  - 상태별 컬러 뱃지 (완료/실패/진행 중 등)
  - 클릭 시 해당 단계로 자동 이동
  - 삭제 버튼 (`DELETE /api/jobs/{id}`)

---

### 8. 최종 렌더링 버튼 불작동 수정
- **원인**: Pydantic v2에서 `model_json()` 미존재
- **수정**: `model_json()` → `model_dump_json()` 전환

---

### 9. 렌더링 진행 중 멈춤 현상 최적화
- **원인 1**: `boxblur` 필터가 원본 해상도에서 동작 → 세로 변환 수분 소요
- **원인 2**: `--reload` 모드에서 파일 변경 감지 시 서버 리로드 → 백그라운드 렌더 태스크 강제 종료
- **수정** (`effects_engine.py`):
  - 배경을 1/4 해상도로 처리 후 확대, `avgblur=10` 사용
  - 모든 preset `ultrafast`로 변경
  - `-ss` input seeking으로 이동 (출력 seeking → 전체 디코딩 방지)
  - `-c copy` 스트림 복사
- **서버 기동**: `--reload` 옵션 제거 → `nohup uvicorn main:app --port 8001`

---

### 10. 하이라이트9회말 스타일 효과 추가
- **참고 채널**: 다양한 화면 분할·클로즈업 효과 차용
- **신규 효과** (`effects_engine.py`, `schemas.py`):
  - `closeup_fill`: 클로즈업으로 화면 채우기
  - `split_top_bottom`: 상하 2분할
  - `split_left_right`: 좌우 2분할
  - `zoom_punch`: 줌 펀치 효과
  - `shake`: 흔들기 효과
- `Effect` 모델에 `crop_x: float = 0.5`, `crop_y: float = 0.5` 추가
- `_apply_complex_effect` 함수 추가
- 레이아웃 효과 시 비율 변환 스킵 (`has_layout_effect` 체크)
- **프론트엔드** (`EffectsStep.tsx`): 효과 프리셋 8종 UI (이모지 + 설명)

---

### 11. `seg_002.wav` 파일 없음 에러 수정
- **원인**: `afftdn=nf=-15` 설정이 ffmpeg 허용 범위(-80~-20) 초과 → 오디오 추출 자체 실패
- **수정** (`transcriber.py`):
  - `nf=-15` → `nf=-25`
  - `not audio_path.exists()` 체크 추가: 파일 없으면 필터 없이 재추출 폴백

---

### 12. 완성된 영상 다운로드 안 되는 문제 수정
- **원인**: 이전 완료 작업 불러올 때 `outputs` 상태가 비어있어 다운로드 화면 미표시
- **수정** (`RenderStep.tsx`):
  - `job.status === 'completed'`이면 `useEffect`로 자동 `getOutputs()` 호출
  - `outputs` 채운 후 `done` 화면으로 전환

---

## 현재 서버 상태
- **백엔드**: `uvicorn main:app --port 8001` (nohup 실행 중)
- **프론트엔드**: Vite dev server (Port 5174, 핫 리로드)
- **복원된 작업 수**: 5개

---

## 파일 변경 목록
| 파일 | 변경 내용 |
|------|-----------|
| `server/config.py` | FFmpeg PATH, WHISPER_MODEL, FONTS_DIR, dotenv 추가 |
| `server/main.py` | model_dump_json, 리라이팅 API, 작업 복원, 삭제 API |
| `server/services/transcriber.py` | Whisper 정확도 개선, afftdn 수정, 파일 존재 체크 |
| `server/services/effects_engine.py` | 신규 효과 5종, 성능 최적화, input seeking |
| `server/services/rewriter.py` | GPT 리라이팅 서비스 (신규) |
| `server/models/schemas.py` | EffectType 추가, 비율 설정, 폰트 설정 |
| `server/.env` | OPENAI_API_KEY 저장 |
| `server/fonts/GmarketSansTTFBold.ttf` | 폰트 파일 추가 |
| `client/src/pages/InputStep.tsx` | 이전 작업 불러오기 UI |
| `client/src/pages/SubtitleStep.tsx` | GPT 리라이팅 + 인라인 편집 |
| `client/src/pages/EffectsStep.tsx` | 비율 선택 + 효과 프리셋 UI |
| `client/src/pages/RenderStep.tsx` | completed 상태 자동 복원 |
| `client/src/hooks/useApi.ts` | listJobs, deleteJob, rewriteSubtitle, getRewriteStyles |
| `client/src/types/index.ts` | crop_x/y, font_name, aspect_ratio 타입 추가 |
